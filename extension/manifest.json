{
  "manifest_version": 3,
  "name": "Plain-Language Site Risk Checker",
  "version": "0.4.0",
  "description": "Flags risky sites in plain language when you focus a password or payment field. Inspects the rendered page and the traffic it generates, all locally; domain age uses the free, keyless RDAP protocol and fails open if unavailable. No API keys, no backend.",
  "permissions": ["history", "storage", "alarms", "webRequest"],
  "host_permissions": [
    "<all_urls>"
  ],
  "background": {
    "service_worker": "background.js"
  },
  "content_scripts": [
    {
      "matches": ["<all_urls>", "file:///*"],
      "js": ["page-probe.js"],
      "run_at": "document_start",
      "world": "MAIN",
      "all_frames": false
    },
    {
      "matches": ["<all_urls>", "file:///*"],
      "js": ["page-inspection.js", "content.js"],
      "css": ["banner.css"],
      "run_at": "document_idle",
      "all_frames": false
    }
  ]
}
